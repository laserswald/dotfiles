
bind generic S :source ~/.tigrc
bind generic P +git push

## Revising commits

# Revise commits
bind main <Ctrl-r> ?!git rebase -i %(commit)^

# Finish revision
bind main <Ctrl-f> +git rebase --continue

