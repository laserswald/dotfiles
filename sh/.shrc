#!/bin/sh

# Load any files in my shell directory.
for file in ~/.sh.d/*.sh ; do
    source $file
done

# Override defaults with locals.
if [[ -e $HOME/.shrc.local ]]; then
    source $HOME/.shrc.local
fi

